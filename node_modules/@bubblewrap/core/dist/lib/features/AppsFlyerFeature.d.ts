import { EmptyFeature } from './EmptyFeature';
export declare type AppsFlyerConfig = {
    enabled: boolean;
    appsFlyerId: string;
};
export declare class AppsFlyerFeature extends EmptyFeature {
    constructor(config: AppsFlyerConfig);
}
